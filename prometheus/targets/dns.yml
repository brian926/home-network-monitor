- targets:
    - 192.168.1.2      # PLACEHOLDER — replace with real Pi-hole address
  labels:
    module: dns_pihole
    role: local_dns

- targets:
    - 1.1.1.1
    - 8.8.8.8
  labels:
    module: dns_upstream
    role: upstream_dns
